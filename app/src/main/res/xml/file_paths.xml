<?xml version="1.0" encoding="utf-8"?>
<!--
  What the FileProvider is allowed to hand out. Only the one cache directory the
  share sheet writes into — nothing else in the app's storage is exposed.
-->
<paths>
    <cache-path name="shared" path="shared/" />
</paths>
